{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            // "type": "shell",
            // "type": "process",
            // "command": "echo Hello",
            // "command": "${command:extension.wolf.touchBarStart}",
            // "command": "${command:extension.wolf.barkAtCurrentFile}",
            "command": "${command:wolf.barkAtCurrentFile}",
            // "command": "${command:workbench.action.terminal.toggleTerminal}",
            "runOptions": {
                "runOn": "folderOpen",
            }
        }
    ]
}
